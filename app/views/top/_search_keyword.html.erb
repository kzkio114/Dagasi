<h2 style="text-transform: uppercase !important; font-weight: bold;">検索キーワード: <%= @keyword %></h2>
<% if @keyword.present? %>
  <%= button_to 'キーワードを保存する', add_to_cart_path(@keyword), data: { turbo_action: 'replace' }, class: 'button' %> <!-- カートに入れるボタン -->
<% end %>