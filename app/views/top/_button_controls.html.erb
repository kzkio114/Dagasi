<div class="notification is-primary">
  <p>登録アイテム一覧！！</p>
  <div class="columns is-gapless no-gap">
    <div class="column">
      <%= button_to "説明", show_info_top_index_path, method: :get, data: { turbo_frame: "info_area" }, class: 'button is-warning' %>
    </div>
    <div class="column">
      <%= button_to '開く', add_button_path, method: :post, data: { turbo_frame: "buttons" }, class: 'button is-info tight-button' %>
    </div>
    <div class="column">
      <%= button_to "閉じる", remove_buttons_path, method: :delete, data: { turbo_frame: "buttons" }, class: 'button is-danger tight-button' %>
    </div>
    <div class="column">
    <%= link_to "結果をTwitterで共有する", "https://twitter.com/intent/tweet?text=#{CGI.escape(@tweet_text)}", target: "_blank", class: "button is-primary", role: "button" %>
　 </div>
  </div>
</div>
<%= turbo_frame_tag "buttons" do %>
<% end %> <!-- ボタンコントロール -->

